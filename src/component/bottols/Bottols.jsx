import React from 'react';
import {use} from 'react';
const Bottols = ({bottolsPromise}) => {
    const bottols=use(bottolsPromise)
console.log(bottols)  
    return (
        <div>
            {
               
                    
                
            }
            
        </div>
    );
};

export default Bottols;